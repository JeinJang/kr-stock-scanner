# KRX Scanner Configuration
scanner:
  # markets: ["KOSPI", "KOSDAQ", "ETF"]
  markets: ["KOSPI", "KOSDAQ"]
  lookback_days: 250
  max_ai_analyze: 50

news:
  provider: "naver_api"
  max_articles_per_stock: 5
  search_days: 3

ai:
  model: "gpt-5-nano"
  max_tokens: 600

telegram:
  enabled: true

schedule:
  run_time: "16:00"
  timezone: "Asia/Seoul"
